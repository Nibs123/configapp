# Betaflight Configurator offline packaging

1. Download a Betaflight Configurator release that contains static web assets.
2. Extract into `tools/betaflight/webapp/` so `index.html` exists.
3. Launcher opens it in Chromium app mode at `http://127.0.0.1:8810`.
